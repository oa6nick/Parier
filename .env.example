# Pariall - Environment variables
# Copy to .env and adjust for your environment
# PRODUCTION: See docs/PRODUCTION.md for deployment checklist

# ============ Frontend (Next.js) ============
NEXT_PUBLIC_API_URL=http://localhost:28090
# PRODUCTION: Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# PRODUCTION: Never leave empty; set real client secret from Keycloak
KEYCLOAK_CLIENT_SECRET=
KEYCLOAK_SERVER_URL=http://localhost:28080
KEYCLOAK_ISSUER=http://localhost:28080/realms/parier
KEYCLOAK_JWT_ISSUER=http://keycloak:8080/realms/parier
KEYCLOAK_CERT_ENDPOINT=http://keycloak:8080/realms/parier/protocol/openid-connect/certs
KEYCLOAK_DEFAULT_REALM=parier
KEYCLOAK_CLIENT_ID=parier-api

# ============ Database ============
DB_API_USERNAME=postgres
DB_API_PASSWORD=password

# ============ Keycloak ============
KEYCLOAK_SERVER_URL=http://localhost:28080
KEYCLOAK_DEFAULT_REALM=parier
KEYCLOAK_DB_USERNAME=keycloak
KEYCLOAK_DB_PASSWORD=keycloak
KEYCLOAK_BOOTSTRAP_ADMIN_USERNAME=admin
KEYCLOAK_BOOTSTRAP_ADMIN_PASSWORD=admin

# ============ MinIO ============
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# ============ API Server ============
# PRODUCTION: Change to a strong random value (openssl rand -base64 32)
STORE_SECRET=your-super-secret-jwt-key-change-in-production
FRONTEND_BASE_URL=http://localhost:3000
# Optional: comma-separated CORS origins (default: FRONTEND_BASE_URL)
# CORS_ALLOW_ORIGINS=https://app.example.com,https://www.example.com
# Rate limiting (requests per second, burst size)
RATE_LIMIT_RPS=10
RATE_LIMIT_BURST=20

# ============ Docker Compose ============
KC_HOSTNAME=http://localhost:28080
KC_HOSTNAME_ADMIN=http://localhost:28080
